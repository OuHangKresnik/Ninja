package ouhang.decisionmodel.model.examples;

import java.util.Math;

input {
  signal1 : double;
  signal2 : int;
  signal3 : long;
  signal4 : string;
  signal5 : double;
}

func double minus(double a, double b) {
  return Math.minus(a, b);
}

model double subModel {
  return minus(signal1, signal2);
}

model string mainModel {
  return "APPROVE";
}
